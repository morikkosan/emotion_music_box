name: Rails CI with Postgres

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      db:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
    steps:
      - name: Install tools for connectivity checks
        run: |
          sudo apt-get update
          sudo apt-get install -y netcat-openbsd dnsutils

      - name: Wait for Postgres to be ready
        run: |
          for i in {1..20}; do
            nc -z db 5432 && echo "🎉 Postgres is up!" && exit 0
            echo "Waiting for Postgres... ($i)"
            sleep 2
          done
          echo "❌ Postgres never came up" && exit 1

      - name: Check DB hostname resolution
        run: |
          echo "==== dig db ===="
          dig db

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install gems
        run: bundle install --jobs 4 --retry 3

      - name: Prepare database (retry on localhost if failed)
        env:
          RAILS_ENV: test
        run: |
          set -e
          echo "🎲 db(サービス名)で db:create 実行"
          if bundle exec rails db:create; then
            echo "✅ db(サービス名)で成功"
          else
            echo "❗ db(サービス名)で失敗 → localhost で再実行します"
            export PGHOST=localhost
            bundle exec rails db:create
          fi
          echo "🎲 db(サービス名)で schema:load 実行"
          if bundle exec rails db:schema:load; then
            echo "✅ db(サービス名)で成功"
          else
            echo "❗ db(サービス名)で失敗 → localhost で再実行します"
            export PGHOST=localhost
            bundle exec rails db:schema:load
          fi

      - name: Run RSpec tests
        env:
          RAILS_ENV: test
        run: bundle exec rspec
