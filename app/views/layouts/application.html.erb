<!DOCTYPE html>
<html>

<head>
  <title><%= content_for(:title) || "EMOTION MUSIC BOX" %></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <link rel="manifest" href="/manifest.json">
  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <!-- ▼ アプリケーションCSS -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <!-- ▼ アプリケーションJS。module + defer でHTMLパースをブロックしない -->
  <%= javascript_include_tag "application",
  "data-turbo-track": "reload",
  type: "module",
  defer: true %>



  <!-- ▼ Font Awesome（アイコン）-->
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

  <!-- ▼ Google Fonts（フォント） -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
        rel="stylesheet">

  <!-- ▼ GSAP（アニメーション用） -->
  <script defer
          src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.9.1/gsap.min.js"></script>

  <!-- ▼ SweetAlert2（ポップアップライブラリ） -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11" defer></script>

</head>

<!-- ✅ ここが修正ポイント！ -->
<body data-flash-notice="<%= j flash.now[:notice] %>" data-flash-alert="<%= j flash.now[:alert] %>">

  <p>🔍 Debug: <%= current_user.inspect %></p>

  <div id="comment-container"></div>

  <%= yield %>

  <!-- ✅ BGM再生スクリプト -->
  <script>
    function toggleAudio() {
      var audio = document.getElementById('audioPlayer');
      var icon = document.getElementById('audioButtonIcon');
      if (audio.paused) {
        audio.play();
        icon.innerHTML = '❚❚';  // 再生中は停止ボタンアイコン
      } else {
        audio.pause();
        icon.innerHTML = '♪';   // 停止中は再生ボタンアイコン
      }
    }
  </script>

<script>
  window.SOUNDCLOUD_CLIENT_ID = "<%= ENV['SOUNDCLOUD_CLIENT_ID'] %>";
</script>

<p style="color: rgb(241, 239, 239);">音楽は <a href="https://soundcloud.com" target="_blank" rel="noopener noreferrer" >SOUND CLOUD</a> より提供されています。</p>


</body>

</html>
