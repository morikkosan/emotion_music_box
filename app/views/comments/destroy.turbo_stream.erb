<%= turbo_stream.remove "comment_#{@comment.id}" %>
<%= turbo_stream.replace "comment-count",
      partial: "emotion_logs/comment_count",
      locals: { emotion_log: @emotion_log } %>

<turbo-stream action="redirect" url="<%= emotion_log_path(@emotion_log) %>"></turbo-stream>
